path: /home/student/Synthetic-data/out
train: train/images
val: val/images

# 5 keypoints, each as (x,y,v) -> dimension=3
kpt_shape: [5, 3]


flip_idx: [0, 3, 4, 1, 2]  # 0 stays itself always, 1 <-> 3 and 2 <-> 4 are swapping pairs


names:
  0: needle_holder
  1: tweezers
